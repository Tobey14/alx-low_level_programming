#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS.
 */
int _putchar(char c)
{
    return (EXIT_SUCCESS);
}

int _islower(int c){
    return (EXIT_SUCCESS);
};

int _isalpha(int c){
    return (EXIT_SUCCESS);
}

int _abs(int n){
    return (EXIT_SUCCESS);
};

int _isupper(int c){
    return (EXIT_SUCCESS);
};
