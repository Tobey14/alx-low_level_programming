#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always EXIT_SUCCESS.
 */

int _isdigit(int c){
    return (EXIT_SUCCESS);
};

int _strlen(char *s){
    return (EXIT_SUCCESS);
};

void _puts(char *s){
    return (EXIT_SUCCESS);
};

char *_strcpy(char *dest, char *src){
    return (EXIT_SUCCESS);
};

int _atoi(char *s){
    return (EXIT_SUCCESS);
};
