#include "main.h"
#include <stdio.h>

/**
 * main - check the code
 *
 * Return: Always arg[0].
 */

int _isdigit(int c){
    return (c);
};

int _strlen(char *s){
    return (5);
};

void _puts(char *s){

};

char *_strcpy(char *dest, char *src){
    return (dest);
};

int _atoi(char *s){
    return (5);
};
